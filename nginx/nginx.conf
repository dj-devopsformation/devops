events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index day1.html;
        }

        location /nginx_status {
            stub_status;
            allow all;
        }
    }
}
